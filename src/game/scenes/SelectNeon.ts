import { Scene } from "phaser";
import { Button } from "../components/Button";
import window_config from "../config/window_config";


export class SelectNeon extends Scene {

    constructor() {
        super({ key: 'SelectNeon' });
    }

    create() {
        

    }
} 